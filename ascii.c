#include<stdio.h>
/**
 * @brief this is to test out the character's value that is
 * convertable from int to str
 */
int main(){

  int i;

    for(i=0;i<=255;i++)
         printf("%c: %d\n",i,i);

    return 0;
}
